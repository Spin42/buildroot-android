config BR2_PACKAGE_ADSP_BOOT
	bool "adsp-boot"
	help
	  Init script to boot the ADSP remoteproc after all required
	  services (pd_mapper, tqftpserv, rmtfs, hexagonrpc) are ready.
	  Loads required kernel modules and starts the ADSP.
